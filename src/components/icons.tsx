export function Logo({ width = 32, height = 32, ...props }) {
  return (
    <svg
      width={width}
      height={height}
      viewBox="0 0 32 32"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      {...props}
    >
      {/* Bạn có thể dán mã SVG của mình vào đây */}
    </svg>
  );
}
